---
import i18next from "i18next";

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time>
  {
    date.toLocaleDateString(i18next.language, {
      year: "numeric",
      month: "long",
      day: "numeric",
    })
  }
</time>
